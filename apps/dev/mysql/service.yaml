apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: rbac-dashboard
spec:
  clusterIP: None   # headless — required for StatefulSet DNS
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
